{{> header}}
<div class="row">
  <div class="col-12">
    <h1>{{video.title}}</h1>
  </div>
  <div class="col-12">
    {{{video.link}}}
  </div>
</div>
{{#if req.signedCookies.id_token}}
  <div class="row">
    <a href="/videos/{{video.videoId}}/edit" class="btn btn-outline-secondary mt-3">Edit</a>
    <a href="/videos/{{video.videoId}}/delete" class="btn btn-outline-secondary mt-3">Delete</a>
  </div>
{{/if}}
{{> footer}}
