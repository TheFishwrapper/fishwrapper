{{> header}}
  <div class="row">
    <div class="col">
      <h1 id="quiz-title">{{quiz.title}}</h1>
      <h6 class="mb-3">by {{quiz.author}}</h6>
      <p>{{{quiz.blurb}}}</p>
      <div class="image-container text-center" id="quiz-image">
        <img alt="Quiz thumbnail" class="img-fluid" src="{{image quiz.thumbnail}}" />
        <h6>{{quiz.thumbnail_credit}}</h6>
      </div>
      <form action="/quizzes/{{quiz.quizId}}" method="POST">
        {{#each quiz.questions}}
          <h3>{{qContent}}</h3>
          <div class="none">
          {{#each answers}}
            <input type="radio" id="{{aId}}" name="{{../qId}}" value="{{aResult}}" />
            <label for="{{aId}}">{{aContent}}</label><br />
          {{/each}}
          </div>
        {{/each}}
        <input type="submit" class="btn btn-warning submit" value="Submit" />
      </form>

      {{#if req.signedCookies.id_token}}
        <a href="/quizzes/{{quiz.quizId}}/edit" class="btn btn-outline-secondary mt-3">Edit</a>
        <a href="/quizzes/{{quiz.quizId}}/delete" class="btn btn-outline-secondary mt-3">Delete</a>
      {{/if}}
    </div>
  </div>
{{> footer}}
